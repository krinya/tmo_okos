{
  "name": "tmo-okos",
  "version": "1.0.0",
  "private": true,
  "description": "One-page proof-of-ownership site",
  "scripts": {
    "start": "serve public -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
